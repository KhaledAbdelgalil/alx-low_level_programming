#include <stdio.h>
/**
 * main - prints the name of the C file
 * that is compiled and produce that program.
 *
 * Return: Always 0.
 */
int main(void)
{
        printf("%s\n", __FILE__);
        return (0);
}
